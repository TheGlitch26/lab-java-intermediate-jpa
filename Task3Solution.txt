1) Modifying table structure like using ALTER TABLE. In JPA, we can activate ddl-auto, which allows JPA to automatically create columns.
However, if we wanted to change a spesific column, its better to use SQL queries. 

2) Retrieving data from multiple tables might not be efficient. Its better to use SQL queries.

3) Dropping database.